apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rag-backend-storage
  labels:
    app: rag-backend
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-storage-provisioner

# Made with Bob